const port = require('server')
const DiscordAPI = require('DiscordAPI/v9')

getSelection.DiscordAPI.fetch('Data', "784442766234353686 ")
getComputedStyle.fetch(`${fetch.userid}`)

document.getElementById('pfpdc').src = `${fetch.userid.DiscordAPI} + $[ID]`